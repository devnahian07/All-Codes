#include<bits/stdc++.h>

#define int long long

typedef pair<int, int> pll;

using namespace std;

signed main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n, m;
    cin >> n >> m;
    
    return 0;
}