#include<bits/stdc++.h>

#define int long long

using namespace std;

signed main(){
    // string s;
    // cin >> s;
    // int x = 0;
    // for(int i = s.size()-1, j = 0; s[i]>='0' && s[i]<='9'; i--, j++){
    //         x+=((s[i]-'0')*(pow(10, j)));
    //         s.pop_back();
    // }
    // x++;
    // string nm;
    // while(x>0){
    //     char c = '0' + (char)(x%10);
    //     nm.push_back(c);
    //     x/=10;
    // }
    // reverse(nm.begin(), nm.end());
    // cout << (s+nm) << "\n";
    map<char, int> nums;
    nums['c']++;
    cout << nums['c'];
    return 0;
}